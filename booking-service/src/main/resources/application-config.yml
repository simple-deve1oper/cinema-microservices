spring:
  config:
    import: 'configserver:'
  cloud:
    config:
      uri: http://0.0.0.1:8888
      fail-fast: true
      name: booking-service
      headers:
        Authorization: Bearer ${CONFIG_TOKEN}