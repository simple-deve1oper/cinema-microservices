spring:
  flyway:
    locations: classpath:db/migration
    enabled: true
    clean-disabled: true
  cloud:
    config:
      enabled: false

springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html

logging:
  level:
    root: "info"
    dev.file: "debug"

keycloak:
  client-id: gateway-client
  client-secret: lax66b9pOJMmfBEG4e9A8AyKrzRISOSY

eureka:
  client:
    enabled: false

errors:
  file:
    path-exception: Ошибка получения доступа к ресурсу по пути %s
  image:
    id:
      not-found: Запись об изображении с идентификатором %s не найдена
    name:
      already-exists: Файл c именем %s уже существует
    movie-id:
      bad-request: Обновление номеров изображений может происходить только по одному фильму за один запрос
    movie-id-and-number:
      not-found: Запись об изображении с идентификатором фильма %d и порядковым номером %d не найдена
    number:
      bad-request: В переданных объектах запроса номера не должны повторяться
    number-is-ordinal:
      bad-request: В переданных объектах запроса все номера должны быть порядковыми